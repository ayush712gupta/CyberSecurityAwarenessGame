import { useEffect } from "react";

const DFS = () => {
  useEffect(() => {
    window.location.href = "https://digital-footprint-simulator.blogspot.com/";
  }, []);

  return <p>Redirecting...</p>;
};

// eslint-disable-next-line react-refresh/only-export-components
export default DFS;
